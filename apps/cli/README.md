# @envsafe/cli

The envsafe command line tool. This is a placeholder. It is not implemented yet
and is not built. It will be built on top of `@envsafe/crypto` against a frozen
spec, once the architect writes one.
